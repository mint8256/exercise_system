<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.five.dao.UserPaperDao">

    <resultMap type="com.five.entity.UserPaper" id="UserPaperMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="paperId" column="paper_id" jdbcType="INTEGER"/>
        <result property="startTime" column="start_time" jdbcType="TIMESTAMP"/>
        <result property="submitTime" column="submit_time" jdbcType="TIMESTAMP"/>
        <result property="score" column="score" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into exercise_system.user_paper(user_id, paper_id, start_time, submit_time, score)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.paperId}, #{entity.startTime}, #{entity.submitTime}, #{entity.score})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into exercise_system.user_paper(user_id, paper_id, start_time, submit_time, score)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.paperId}, #{entity.startTime}, #{entity.submitTime}, #{entity.score})
        </foreach>
        on duplicate key update
        user_id = values(user_id) , paper_id = values(paper_id) , start_time = values(start_time) , submit_time =
        values(submit_time) , score = values(score)
    </insert>

</mapper>

